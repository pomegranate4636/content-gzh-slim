---
name: content-gzh-distribution-pack
description: Adapt one saved content-gzh-slim WeChat article into an optional cross-platform distribution set after an explicit user request. Internal only; does not search, revise the article or main title, save, review, draft, or publish.
---

# Content GZH Distribution Pack

Use only after the same Run is `saved` and the user explicitly requests a distribution set. Read only the locked article body and final WeChat title from `approved_final.json`, the verified `save_receipt.json`, and the same Context Pack's small frozen IP distribution constraints and forbidden items.

Return the final WeChat title unchanged; for Xiaohongshu, WeChat Channels, and Douyin return one platform title, a 50–100 Chinese-character introduction, and tags; return one benefit-led Moments share message. Preserve the body, facts, IP boundaries, and main title exactly.

Do not search a knowledge base, read a Profile or original path, create another packet or review chain, write back to a backend, enter a draft box, or publish.
