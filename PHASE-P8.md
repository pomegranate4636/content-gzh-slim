# PHASE P8：客户可交付性验收

## 状态

- 当前：完成；唯一 Review 的一次复核 PASS，已按授权合并 main，停止
- 授权：`EXECUTE_PHASE: P8`（本次任务明确授予）
- 固定基线：`feature/content-gzh-slim` 的本地、`origin` 与远端分支均为 `a601132f5f48d1bb4be8e1a9d923aa5c54ec6469`
- 上游结论：P7 独立 PM Review PASS；未合并 main、未打 Tag、未建 Release、未全局安装、未发布内容

## 2026-09-02：content-source-v1 联合改造补充验收

- P8 原验收记录继续作为历史证据，本次不改写原 Run 或原收据。
- 新版已在隔离 Obsidian 完成 Manifest 推导目标、两次 Gate、create-only 保存和回读，保持未发布。
- 新版已由 ZSK 在飞书真实创建私有隔离知识库及基础 Manifest/Profile 索引；公众号使用临时 Registry、明确无 IP，完成两次测试授权 Gate、create-only 保存和回读，保持未发布。
- 口播与公众号共享同一公共合同，但不互相导入运行代码；口播遇飞书 binding 准确停止。
- 当前树与完整 Git 历史的凭据、个人信息和真实飞书对象扫描通过；唯一飞书 URL 历史命中是测试域名。
- 仓库已转 Public；匿名 fresh clone、MIT/发布校验、隔离安装 probe 和本机发布副本回读均通过。内容正文仍保持未发布，仓库公开不等于把任何公众号文章发布到平台。

## 唯一目标

在不重设计或扩大 P1—P7 的前提下，验证该候选对新用户和不同客户知识库的可交付性。所有真实数据、正文、链接 token、Run Artifact 与凭据仅留在临时私有位置；本仓库只保存脱敏的阶段收据。

## 本阶段输入与验收口径

1. **干净安装**：从本固定远端提交创建新的隔离 clone 或 worktree；构建自包含候选，并只安装到全新的项目级测试 Host。P7 的 `.p7-test-host` 不作为通过证据。
2. **两条连续全新 Run**：从 `start` 分别绑定两个不同的合成知识库，并在两个知识库中解析同名 IP。每条只允许一个 Run、一个 Context Pack、两次状态机 Gate、Writer 零次知识库搜索、零 Reviewer，且读取预算不超过 SPEC。
3. **真实后端**：两条 Run 中一条在隔离 Obsidian create-only 保存并回读；另一条仅在已有明确授权的受控飞书测试目标中创建一个测试文档并回读。无目标、权限或来源证据即 fail closed，不复制凭据。
4. **保存后分发**：只对一条已保存 Run，在本阶段已授权的 PM 代理触发精确请求 `生成分发包` 后调用分发 Skill；验证全平台物料完整、正文与 Gate B 的公众号主标题摘要保持不变，且不检索知识库、不保存草稿箱、不发布。
5. **长度复测**：另一条 Run 明确要求约 5000 个中文字符，记录实际字符数和偏差；只诊断，不因非 SPEC 阻断的偏差无限修复。

## P8 合成验收的代理 Gate 与停止点

- Runtime 仍必须真实到达 `waiting_direction` 和 `waiting_final`；不得跳过状态机、篡改 Run 或伪造已确认状态。
- 仅限 P8 的合成、非客户验收数据，用户明确授权当前任务的 PM 依据下列标准代理作出合同要求的精确决定文本：Gate A 使用 `确认方向`，Gate B 使用 `确认正文和标题`，分发使用 `生成分发包`。
- 回执必须记录 `decision_origin=user_authorized_pm_delegation`，不得写成用户逐条真人确认。
- Gate A 只检查知识库/IP 绑定、同库来源、合成事实边界和干货结构；Gate B 只检查冻结方向、无跨库指纹、无占位符或虚构真人经历、Top 3 标题绑定和保存预览。不得按主观文采反复改稿。
- 该例外不改变正式产品：面向真实用户的任何 Run 仍必须由用户逐次作出 Gate A 和 Gate B 的合法决定。P7 的真实逐 Gate 证据保持原结论。

## 严格边界

- 不改动 P1—P7 产品设计、不增加 Skill、Gate、Context Pack、Reviewer、Schema 或常驻状态。
- 不写入 `~/.codex/skills`，不替换或调用 `shu-gongzhonghao-v1`，不改客户 01—05，不复制登录凭据。
- 不把真实客户内容、知识库路径、链接 token、Run Artifact、正文或凭据写入 Git；回执仅包含脱敏计数、状态和摘要哈希。
- P8 最多一次独立 PM Review；若 CHANGES REQUESTED，最多一次最小修复和一次复核。仅处理真实业务失败、串库/IP、越权读取、Gate 绕过、已确认正文/标题破坏、敏感数据泄漏或超复杂度。
- 唯一独立 P8 Review 的一次复核已 PASS，已通过可审计 PR 合并并推送 `main`；未打 Tag、未建 Release、未写入全局 Skill、未替换 V1、未进入草稿箱或发布。

## 执行顺序

1. P8-1：提交并推送本设计卡及真实状态更新，固定执行基线。
2. P8-2：在干净环境完成候选构建、项目级安装、probe 与隐私边界检查，生成脱敏安装回执。
3. P8-3：使用已授权的两个合成隔离知识库，连续完成两条新 Run；每次在状态机等待点按代理 Gate 标准审阅。
4. P8-4：在一条已保存 Run 上按代理授权生成一次分发包，并记录约 5000 字长度诊断。
5. P8-5：固定提交，完成唯一独立 PM Review 与最终边界检查；若 PASS，按用户授权合并 `main` 后停止。

## 已知历史事项

- P7 的原始第二条 Run 在 Context 创建前因冻结字段不一致被 fail-closed；P8 不修补或重解释该 Run，新的两条 Run 必须各自首次连续完成。
- P6/P7 的约 5000 字偏差是 P8 的诊断项，除非触发 SPEC 硬阻断，不扩展为第二轮或无限修复。
- P8 原始 Run A 在首次 Context 装配时因选择文本与冻结分析不逐字一致被拒绝；Run B 在首次 Gate B 前因正文合同对普通句中“状态：”误拦截被拒绝。两项原始失败均永久保留，不计为连续成功。
- P8 唯一 Review 已提出一次可复现 Runtime 正文合同修复。修复后的唯一 recheck 必须创建全新 A2/B2，从 `start` 各跑一次；不得恢复、续写或重试原始 A/B。若 A2 或 B2 再失败，停止、不做第二轮修复、不合并 main。

## 本阶段当前未完成项

- 干净安装已通过；原始 A/B 首次失败、唯一 Review 的 CHANGES REQUESTED 与一轮最小修复均已入回执。
- A2/B2 已从全新 RunStore 连续完成，Obsidian/飞书回读与保存后分发均已完成；约 5000 字实际 3097 个中文字符，仅作为诊断。唯一 Review 复核已 PASS，PR 已合并 main；不启动 P9 或额外美化。
